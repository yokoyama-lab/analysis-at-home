<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (takec k l) <= length l` (mirror the Rocq target's definitions). Mirror
`work-units/list-take-steps/targets/rocq/ListTake.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
