<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(l1 ++ l2) ++ l3 = l1 ++ (l2 ++ l3)` (mirror the Rocq target). Mirror
`work-units/list-append-assoc/targets/rocq/AppAssoc.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
