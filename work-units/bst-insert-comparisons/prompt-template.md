<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `snd (insertc x t) <= S (lheight t)` (mirror the Rocq target's tree definitions). Mirror
`work-units/bst-insert-comparisons/targets/rocq/BstInsert.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
