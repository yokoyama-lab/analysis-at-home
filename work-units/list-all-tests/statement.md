# Universal check uses at most n tests

## Claim
> `snd (allc p l) <= length l`.

## Cost model
Reference Rocq proof (`list_all_tests`, axiom-free): [`targets/rocq/ListAll.v`](targets/rocq/ListAll.v).
