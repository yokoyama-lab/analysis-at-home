<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (allc p l) <= length l` (mirror the Rocq target's definitions). Mirror
`work-units/list-all-tests/targets/rocq/ListAll.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
