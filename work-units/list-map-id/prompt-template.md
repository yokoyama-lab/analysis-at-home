<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `map (fun x => x) l = l` (mirror the Rocq target). Mirror
`work-units/list-map-id/targets/rocq/MapId.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
