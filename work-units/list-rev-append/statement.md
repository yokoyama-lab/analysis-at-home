# rev distributes over append (reversed)

## Claim
> `rev (l1 ++ l2) = rev l2 ++ rev l1`.

## Cost model
Reference Rocq proof (`list_rev_append`, axiom-free): [`targets/rocq/RevApp.v`](targets/rocq/RevApp.v).
