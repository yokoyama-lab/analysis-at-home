<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `fact (S n) = S n * fact n` (mirror the Rocq target). Mirror
`work-units/factorial-recurrence/targets/rocq/FactRec.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
