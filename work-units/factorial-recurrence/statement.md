# Factorial recurrence: (n+1)! = (n+1) * n!

## Claim
> `fact (S n) = S n * fact n`.

## Cost model
Reference Rocq proof (`factorial_recurrence`, axiom-free): [`targets/rocq/FactRec.v`](targets/rocq/FactRec.v).
