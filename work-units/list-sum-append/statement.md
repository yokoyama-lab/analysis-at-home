# Sum distributes over append

## Claim
> `lsum (l1 ++ l2) = lsum l1 + lsum l2`.

## Cost model
Reference Rocq proof (`list_sum_append`, axiom-free): [`targets/rocq/SumApp.v`](targets/rocq/SumApp.v).
