<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `lsum (l1 ++ l2) = lsum l1 + lsum l2` (mirror the Rocq target). Mirror
`work-units/list-sum-append/targets/rocq/SumApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
