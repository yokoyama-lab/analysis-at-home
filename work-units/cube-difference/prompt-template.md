<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(S n) * (S n) * (S n) = n * n * n + (3 * n * n + 3 * n + 1)` (mirror the Rocq target). Mirror
`work-units/cube-difference/targets/rocq/CubeDiff.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
