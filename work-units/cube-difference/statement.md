# (n+1)^3 - n^3 = 3n^2+3n+1

## Claim
> `(S n) * (S n) * (S n) = n * n * n + (3 * n * n + 3 * n + 1)`.

## Cost model
Reference Rocq proof (`cube_difference`, axiom-free): [`targets/rocq/CubeDiff.v`](targets/rocq/CubeDiff.v).
