# Gauss summation

## Claim
> `2 * sum_upto n = n * (n + 1)`  (i.e. `1+2+...+n = n(n+1)/2`).

## Cost model
`func-ops`, `counts = ["addition"]`. Reference Rocq proof (`gauss_sum`, axiom-free): [`targets/rocq/GaussSum.v`](targets/rocq/GaussSum.v).
