<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length a = length b -> addc a b = length a` (mirror the Rocq target). Mirror
`work-units/classical-addition/targets/rocq/ClassicalAdd.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
