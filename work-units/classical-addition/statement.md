# Multi-precision addition uses n digit-additions

## Claim
> `length a = length b -> addc a b = length a`.

## Cost model
Reference Rocq proof (`classical_addition`, axiom-free): [`targets/rocq/ClassicalAdd.v`](targets/rocq/ClassicalAdd.v).
