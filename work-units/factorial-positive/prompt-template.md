<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `1 <= fact n` (mirror the Rocq target). Mirror
`work-units/factorial-positive/targets/rocq/FactPos.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
