<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `a mod (S b) < S b` (mirror the Rocq target). Mirror
`work-units/mod-upper-bound/targets/rocq/ModBound.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
