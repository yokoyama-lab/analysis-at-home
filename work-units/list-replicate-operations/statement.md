# Replicate builds n elements with n operations

## Claim
> `snd (replicatec n x) = n`.

## Cost model
Reference Rocq proof (`list_replicate_operations`, axiom-free): [`targets/rocq/ListReplicate.v`](targets/rocq/ListReplicate.v).
