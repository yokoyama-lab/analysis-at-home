<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (replicatec n x) = n` (mirror the Rocq target's definitions). Mirror
`work-units/list-replicate-operations/targets/rocq/ListReplicate.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
