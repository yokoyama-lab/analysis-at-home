<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `binom (S n) (S k) = binom n k + binom n (S k)` (mirror the Rocq target). Mirror
`work-units/pascal-rule/targets/rocq/PascalRule.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
