# Pascal's rule

## Claim
> `binom (S n) (S k) = binom n k + binom n (S k)`.

## Cost model
Reference Rocq proof (`binom_pascal`, axiom-free): [`targets/rocq/PascalRule.v`](targets/rocq/PascalRule.v).
