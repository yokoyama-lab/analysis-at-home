# reverse preserves length

## Claim
> `length (rev l) = length l`.

## Cost model
Reference Rocq proof (`list_rev_length`, axiom-free): [`targets/rocq/ListRevLength.v`](targets/rocq/ListRevLength.v).
