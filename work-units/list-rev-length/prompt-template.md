<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `length (rev l) = length l` (mirror the Rocq target's definitions). Mirror
`work-units/list-rev-length/targets/rocq/ListRevLength.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
