<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `lsize (fst (insertc x t)) <= S (lsize t)` (mirror the Rocq target). Mirror
`work-units/bst-insert-size/targets/rocq/BstInsertSize.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
