<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `loop v (loop v (loop v 1)) = v * v * v` (mirror the Rocq target). Mirror
`work-units/warshall-operations/targets/rocq/Warshall.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
