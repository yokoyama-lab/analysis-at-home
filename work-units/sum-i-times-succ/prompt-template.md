<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `3 * sumprod n = n * (n + 1) * (n + 2)` (mirror the Rocq target). Mirror
`work-units/sum-i-times-succ/targets/rocq/SumISucc.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
