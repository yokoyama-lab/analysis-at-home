# append length is additive

## Claim
> `length (l1 ++ l2) = length l1 + length l2`.

## Cost model
Reference Rocq proof (`list_append_length`, axiom-free): [`targets/rocq/ListAppendLength.v`](targets/rocq/ListAppendLength.v).
