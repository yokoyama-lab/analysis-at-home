<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `length (l1 ++ l2) = length l1 + length l2` (mirror the Rocq target's definitions). Mirror
`work-units/list-append-length/targets/rocq/ListAppendLength.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
