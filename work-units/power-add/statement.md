# Law of exponents: a^(m+n) = a^m * a^n

## Claim
> `a ^ (m + n) = a ^ m * a ^ n`.

## Cost model
Reference Rocq proof (`power_add`, axiom-free): [`targets/rocq/PowerAdd.v`](targets/rocq/PowerAdd.v).
