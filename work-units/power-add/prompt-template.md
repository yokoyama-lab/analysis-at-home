<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `a ^ (m + n) = a ^ m * a ^ n` (mirror the Rocq target). Mirror
`work-units/power-add/targets/rocq/PowerAdd.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
