<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `map f (l1 ++ l2) = map f l1 ++ map f l2` (mirror the Rocq target). Mirror
`work-units/list-map-append/targets/rocq/MapApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
