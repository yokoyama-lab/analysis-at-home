<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `a <= b -> a*c <= b*c` (mirror the Rocq target). Mirror
`work-units/mult-monotone/targets/rocq/MultMono.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
