<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `lsize (map_tree f t) = lsize t` (mirror the Rocq target's tree definitions). Mirror
`work-units/tree-map-size/targets/rocq/TreeMapSize.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
