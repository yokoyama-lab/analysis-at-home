# Mapping over a tree preserves its size

## Claim
> `lsize (map_tree f t) = lsize t`.

## Cost model
Reference Rocq proof (`map_tree_size`, axiom-free): [`targets/rocq/TreeMapSize.v`](targets/rocq/TreeMapSize.v).
