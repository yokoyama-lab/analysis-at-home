# Multiplication is monotone in both arguments

## Claim
> `a<=b -> c<=d -> a*c <= b*d`.

## Cost model
Reference Rocq proof (`mult_le_mono_both`, axiom-free): [`targets/rocq/MulMonoBoth.v`](targets/rocq/MulMonoBoth.v).
