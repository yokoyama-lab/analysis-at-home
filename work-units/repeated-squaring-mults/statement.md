# x^(2^k) by repeated squaring uses k multiplications

## Claim
> `snd (repsq k x) = k`.

## Cost model
Reference Rocq proof (`repsq_mults`, axiom-free): [`targets/rocq/RepSquare.v`](targets/rocq/RepSquare.v).
