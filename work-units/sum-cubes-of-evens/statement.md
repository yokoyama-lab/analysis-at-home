# Sum of cubes of the first n even numbers

## Claim
> `sce n = 2 * (n*(n+1)) * (n*(n+1))`.

## Cost model
Reference Rocq proof (`sum_cubes_of_evens`, axiom-free): [`targets/rocq/SumCubesEvens.v`](targets/rocq/SumCubesEvens.v).
