# General arithmetic progression sum

## Claim
> `2 * sad a d n + d * n = 2 * n * a + d * n * n`.

## Cost model
Reference Rocq proof (`sum_arithmetic_progression`, axiom-free): [`targets/rocq/ArithProg.v`](targets/rocq/ArithProg.v).
