# Internal nodes < size

## Claim
> `internal t < size t`.

## Cost model
Reference Rocq proof (`tree_internal_lt_size`, axiom-free): [`targets/rocq/InternalLtSize.v`](targets/rocq/InternalLtSize.v).
