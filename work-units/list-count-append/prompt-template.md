<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `occ x (l1 ++ l2) = occ x l1 + occ x l2` (mirror the Rocq target). Mirror
`work-units/list-count-append/targets/rocq/CountApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
