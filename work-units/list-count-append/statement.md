# Occurrence count distributes over append

## Claim
> `occ x (l1 ++ l2) = occ x l1 + occ x l2`.

## Cost model
Reference Rocq proof (`list_count_append`, axiom-free): [`targets/rocq/CountApp.v`](targets/rocq/CountApp.v).
