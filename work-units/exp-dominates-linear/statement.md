# n < 2^n

## Claim
> `n < 2 ^ n`.

The exponential strictly dominates the identity — the basis for `⌊lg n⌋`-style bounds.

## Cost model
Reference Rocq proof (`exp_dominates_linear`, axiom-free): [`targets/rocq/ExpLinear.v`](targets/rocq/ExpLinear.v).
