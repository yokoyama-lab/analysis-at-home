<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `concat (ls1 ++ ls2) = concat ls1 ++ concat ls2` (mirror the Rocq target). Mirror
`work-units/list-concat-append/targets/rocq/ConcatApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
