<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (filter p l) <= length l` (mirror the Rocq target). Mirror
`work-units/filter-length-le/targets/rocq/FilterLen.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
