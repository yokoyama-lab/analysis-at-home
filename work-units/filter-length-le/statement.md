# filter never lengthens a list

## Claim
> `length (filter p l) <= length l`.

## Cost model
Reference Rocq proof (`filter_length_le`, axiom-free): [`targets/rocq/FilterLen.v`](targets/rocq/FilterLen.v).
