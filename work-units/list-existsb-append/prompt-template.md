<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `existsb p (l1 ++ l2) = existsb p l1 || existsb p l2` (mirror the Rocq target). Mirror
`work-units/list-existsb-append/targets/rocq/ExistsbApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
