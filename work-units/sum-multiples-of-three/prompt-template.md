<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `2 * sm3 n = 3 * (n * (n + 1))` (mirror the Rocq target). Mirror
`work-units/sum-multiples-of-three/targets/rocq/SumMult3.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
