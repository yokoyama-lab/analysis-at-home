# Sum of the first n multiples of 3

## Claim
> `2 * sm3 n = 3 * (n * (n + 1))`.

## Cost model
Reference Rocq proof (`sum_multiples_of_three`, axiom-free): [`targets/rocq/SumMult3.v`](targets/rocq/SumMult3.v).
