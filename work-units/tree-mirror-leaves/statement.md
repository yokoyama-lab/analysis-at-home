# Mirroring preserves the leaf count

## Claim
> `leaves (mirror t) = leaves t`.

## Cost model
Reference Rocq proof (`mirror_leaves`, axiom-free): [`targets/rocq/MirrorLeaves.v`](targets/rocq/MirrorLeaves.v).
