# Consecutive triangular numbers sum to a square

## Claim
> `tri n + tri (S n) = (S n) * (S n)`.

## Cost model
Reference Rocq proof (`triangular_sum_square`, axiom-free): [`targets/rocq/TriSumSquare.v`](targets/rocq/TriSumSquare.v).
