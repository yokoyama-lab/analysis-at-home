<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `tri n + tri (S n) = (S n) * (S n)` (mirror the Rocq target). Mirror
`work-units/triangular-sum-square/targets/rocq/TriSumSquare.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
