<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(S n) * (S n) = n * n + (2 * n + 1)` (mirror the Rocq target). Mirror
`work-units/consecutive-squares-difference/targets/rocq/SquaresDiff.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
