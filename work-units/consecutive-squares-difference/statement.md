# (n+1)^2 - n^2 = 2n+1

## Claim
> `(S n) * (S n) = n * n + (2 * n + 1)`.

## Cost model
Reference Rocq proof (`consecutive_squares_difference`, axiom-free): [`targets/rocq/SquaresDiff.v`](targets/rocq/SquaresDiff.v).
