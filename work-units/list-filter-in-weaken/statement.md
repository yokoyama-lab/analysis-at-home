# filter only removes elements

## Claim
> `In x (filter p l) -> In x l`.

## Cost model
Reference Rocq proof (`list_filter_In_weaken`, axiom-free): [`targets/rocq/FilterInWeaken.v`](targets/rocq/FilterInWeaken.v).
