<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `6 * sumtri n = n * (n + 1) * (n + 2)` (mirror the Rocq target). Mirror
`work-units/sum-of-triangulars/targets/rocq/SumTriangulars.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
