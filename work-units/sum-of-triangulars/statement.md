# Sum of triangular numbers = n(n+1)(n+2)/6

## Claim
> `6 * sumtri n = n * (n + 1) * (n + 2)`.

## Cost model
Reference Rocq proof (`sum_triangulars`, axiom-free): [`targets/rocq/SumTriangulars.v`](targets/rocq/SumTriangulars.v).
