# Zipping uses at most |l1| operations

## Claim
> `snd (zipc l1 l2) <= length l1`.

## Cost model
Reference Rocq proof (`list_zip_operations`, axiom-free): [`targets/rocq/ListZip.v`](targets/rocq/ListZip.v).
