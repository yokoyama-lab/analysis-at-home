<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (zipc l1 l2) <= length l1` (mirror the Rocq target's definitions). Mirror
`work-units/list-zip-operations/targets/rocq/ListZip.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
