<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `fact n <= fact (S n)` (mirror the Rocq target). Mirror
`work-units/factorial-monotone/targets/rocq/FactMono.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
