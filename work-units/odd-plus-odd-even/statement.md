# Odd + odd = even

## Claim
> `(exists k, a = 2*k+1) -> (exists k, b = 2*k+1) -> exists k, a + b = 2*k`.

## Cost model
Reference Rocq proof (`odd_plus_odd_even`, axiom-free): [`targets/rocq/OddPlusOdd.v`](targets/rocq/OddPlusOdd.v).
