<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (l ++ [x]) = S (length l)` (mirror the Rocq target). Mirror
`work-units/list-snoc-length/targets/rocq/SnocLen.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
