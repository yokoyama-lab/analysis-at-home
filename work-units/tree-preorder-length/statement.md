# Preorder traversal lists every node

## Claim
> `length (preorder t) = lsize t`.

## Cost model
Reference Rocq proof (`preorder_length`, axiom-free): [`targets/rocq/PreorderLength.v`](targets/rocq/PreorderLength.v).
