<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (preorder t) = lsize t` (mirror the Rocq target's tree definitions). Mirror
`work-units/tree-preorder-length/targets/rocq/PreorderLength.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
