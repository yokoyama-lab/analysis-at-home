<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `lheight (map_tree f t) = lheight t` (mirror the Rocq target). Mirror
`work-units/tree-map-height/targets/rocq/TreeMapHeight.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
