# Mapping over a tree preserves height

## Claim
> `lheight (map_tree f t) = lheight t`.

## Cost model
Reference Rocq proof (`tree_map_height`, axiom-free): [`targets/rocq/TreeMapHeight.v`](targets/rocq/TreeMapHeight.v).
