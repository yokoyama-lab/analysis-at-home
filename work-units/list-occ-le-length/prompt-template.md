<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `occ x l <= length l` (mirror the Rocq target). Mirror
`work-units/list-occ-le-length/targets/rocq/OccLeLen.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
