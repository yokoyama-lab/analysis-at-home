# Occurrence count is at most the length

## Claim
> `occ x l <= length l`.

## Cost model
Reference Rocq proof (`list_occ_le_length`, axiom-free): [`targets/rocq/OccLeLen.v`](targets/rocq/OccLeLen.v).
