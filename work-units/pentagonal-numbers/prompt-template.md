<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `2 * pent n + n = 3 * n * n` (mirror the Rocq target). Mirror
`work-units/pentagonal-numbers/targets/rocq/Pentagonal.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
