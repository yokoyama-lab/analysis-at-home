<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (repeat x n) = n` (mirror the Rocq target). Mirror
`work-units/repeat-length/targets/rocq/RepeatLen.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
