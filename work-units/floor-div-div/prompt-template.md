<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `a <> 0 -> b <> 0 -> (n / a) / b = n / (a * b)` (mirror the Rocq target). Mirror
`work-units/floor-div-div/targets/rocq/FloorDivDiv.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
