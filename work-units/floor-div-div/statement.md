# Iterated floor division: (n/a)/b = n/(ab)

## Claim
> `a <> 0 -> b <> 0 -> (n / a) / b = n / (a * b)`.

## Cost model
Reference Rocq proof (`floor_div_div`, axiom-free): [`targets/rocq/FloorDivDiv.v`](targets/rocq/FloorDivDiv.v).
