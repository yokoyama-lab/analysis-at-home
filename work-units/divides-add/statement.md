# Divisibility is closed under addition

## Claim
> `divides a b -> divides a c -> divides a (b + c)`.

## Cost model
Reference Rocq proof (`divides_add`, axiom-free): [`targets/rocq/DividesAdd.v`](targets/rocq/DividesAdd.v).
