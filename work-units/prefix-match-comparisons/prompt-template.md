<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `snd (pmatch p t) <= length p` (mirror the Rocq target). Mirror
`work-units/prefix-match-comparisons/targets/rocq/PrefixMatch.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
