# Index-of uses at most n comparisons

## Claim
> `snd (indexofc x l) <= length l`.

## Cost model
Reference Rocq proof (`list_indexof_comparisons`, axiom-free): [`targets/rocq/ListIndexOf.v`](targets/rocq/ListIndexOf.v).
