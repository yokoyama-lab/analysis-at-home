<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `2 ^ n <= 2 ^ (S n)` (mirror the Rocq target). Mirror
`work-units/two-pow-monotone/targets/rocq/TwoPowMono.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
