# Bellman-Ford does V*E relaxations

## Claim
> `loop rounds (loop edges 1) = rounds * edges`.

## Cost model
Reference Rocq proof (`bellman_ford_relaxations`, axiom-free): [`targets/rocq/BellmanFord.v`](targets/rocq/BellmanFord.v).
