# Sequential search uses at most n comparisons

## Claim
> `snd (memberc x l) <= length l`.

## Cost model
Reference Rocq proof (`membership_comparisons`, axiom-free): [`targets/rocq/Membership.v`](targets/rocq/Membership.v).
