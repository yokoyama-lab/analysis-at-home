# odd * odd = odd

## Claim
> `(exists k, a=2*k+1) -> (exists k, b=2*k+1) -> exists k, a*b = 2*k+1`.

## Cost model
Reference Rocq proof (`odd_times_odd_odd`, axiom-free): [`targets/rocq/OddTimes.v`](targets/rocq/OddTimes.v).
