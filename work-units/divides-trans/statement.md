# Divisibility is transitive

## Claim
> `divides a b -> divides b c -> divides a c`.

## Cost model
Reference Rocq proof (`divides_trans`, axiom-free): [`targets/rocq/DividesTrans.v`](targets/rocq/DividesTrans.v).
