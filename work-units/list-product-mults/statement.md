# List product uses n-1 multiplications

## Claim
> `snd (prod_list l) = pred (length l)`.

## Cost model
Reference Rocq proof (`list_product_mults`, axiom-free): [`targets/rocq/ListProduct.v`](targets/rocq/ListProduct.v).
