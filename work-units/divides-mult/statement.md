# A divisor of b divides any multiple of b

## Claim
> `divides a b -> divides a (b * c)`.

## Cost model
Reference Rocq proof (`divides_mult_r`, axiom-free): [`targets/rocq/DividesMult.v`](targets/rocq/DividesMult.v).
