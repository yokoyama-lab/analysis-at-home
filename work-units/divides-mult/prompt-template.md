<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `divides a b -> divides a (b * c)` (mirror the Rocq target). Mirror
`work-units/divides-mult/targets/rocq/DividesMult.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
