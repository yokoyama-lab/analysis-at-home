# Divisibility is antisymmetric

## Claim
> `divides a b -> divides b a -> a = b`.

## Cost model
Reference Rocq proof (`divides_antisym`, axiom-free): [`targets/rocq/DividesAntisym.v`](targets/rocq/DividesAntisym.v).
