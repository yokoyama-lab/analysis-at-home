# Counting matches uses n predicate tests

## Claim
> `snd (countpc p l) = length l`.

## Cost model
Reference Rocq proof (`list_countp_tests`, axiom-free): [`targets/rocq/ListCountP.v`](targets/rocq/ListCountP.v).
