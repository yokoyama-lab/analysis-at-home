# Sum of squares of the first n even numbers

## Claim
> `3 * sse n = 2 * n * (n + 1) * (2 * n + 1)`.

## Cost model
Reference Rocq proof (`sum_squares_of_evens`, axiom-free): [`targets/rocq/SumSqEvens.v`](targets/rocq/SumSqEvens.v).
