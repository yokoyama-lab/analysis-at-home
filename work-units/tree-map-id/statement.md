# Mapping the identity over a tree is the identity

## Claim
> `map_tree (fun x => x) t = t`.

## Cost model
Reference Rocq proof (`tree_map_id`, axiom-free): [`targets/rocq/TreeMapId.v`](targets/rocq/TreeMapId.v).
