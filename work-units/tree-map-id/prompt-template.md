<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `map_tree (fun x => x) t = t` (mirror the Rocq target). Mirror
`work-units/tree-map-id/targets/rocq/TreeMapId.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
