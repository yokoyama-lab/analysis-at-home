<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, length (isort l) = length l (mirror the Rocq target). Mirror
`work-units/insertion-sort-preserves-length/targets/rocq/IsortLength.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
