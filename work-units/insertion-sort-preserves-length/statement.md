# Insertion sort preserves length

## Claim
> `length (isort l) = length l`.

## Cost model
Reference Rocq proof (`isort_preserves_length`, axiom-free): [`targets/rocq/IsortLength.v`](targets/rocq/IsortLength.v).
