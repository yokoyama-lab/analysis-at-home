# An even base raised to a positive power is even

## Claim
> `(exists k, a = 2 * k) -> exists k, a ^ (S n) = 2 * k`.

## Cost model
Reference Rocq proof (`even_power`, axiom-free): [`targets/rocq/EvenPower.v`](targets/rocq/EvenPower.v).
