<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(exists k, a = 2 * k) -> exists k, a ^ (S n) = 2 * k` (mirror the Rocq target). Mirror
`work-units/even-power/targets/rocq/EvenPower.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
