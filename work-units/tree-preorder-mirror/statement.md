# Preorder of the mirror is the reversed postorder

## Claim
> `preorder (lmirror t) = rev (postorder t)`.

## Cost model
Reference Rocq proof (`tree_preorder_mirror`, axiom-free): [`targets/rocq/PreorderMirror.v`](targets/rocq/PreorderMirror.v).
