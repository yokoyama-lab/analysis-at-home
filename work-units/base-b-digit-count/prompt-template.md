<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `2 <= b -> n < b ^ k -> ndigits fuel b n <= k` (mirror the Rocq target). Mirror
`work-units/base-b-digit-count/targets/rocq/DigitCount.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
