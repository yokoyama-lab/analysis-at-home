# Tree height is strictly less than its leaf count

## Claim
> `height t < leaves t`.

## Cost model
Reference Rocq proof (`height_lt_leaves`, axiom-free): [`targets/rocq/HeightLtLeaves.v`](targets/rocq/HeightLtLeaves.v).
