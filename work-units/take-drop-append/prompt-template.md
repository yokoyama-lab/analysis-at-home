<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `take k l ++ drop k l = l` (mirror the Rocq target). Mirror
`work-units/take-drop-append/targets/rocq/TakeDrop.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
