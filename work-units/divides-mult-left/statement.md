# A divisor of b divides any left-multiple

## Claim
> `divides a b -> divides a (c * b)`.

## Cost model
Reference Rocq proof (`divides_mult_l`, axiom-free): [`targets/rocq/DividesMultL.v`](targets/rocq/DividesMultL.v).
