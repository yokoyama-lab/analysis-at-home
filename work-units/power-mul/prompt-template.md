<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `a ^ (m * n) = (a ^ n) ^ m` (mirror the Rocq target). Mirror
`work-units/power-mul/targets/rocq/PowerMul.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
