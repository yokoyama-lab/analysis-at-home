# Mirroring preserves the internal-node count

## Claim
> `internal (mirror t) = internal t`.

## Cost model
Reference Rocq proof (`tree_mirror_internal`, axiom-free): [`targets/rocq/MirrorInternal.v`](targets/rocq/MirrorInternal.v).
