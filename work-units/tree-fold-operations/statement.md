# Folding a tree visits each node once

## Claim
> `snd (sumtree t) = lsize t`.

## Cost model
Reference Rocq proof (`tree_fold_operations`, axiom-free): [`targets/rocq/TreeFold.v`](targets/rocq/TreeFold.v).
