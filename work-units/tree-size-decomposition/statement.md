# Tree size = leaves + internal nodes

## Claim
> `size t = leaves t + internal t`.

## Cost model
Reference Rocq proof (`size_leaves_internal`, axiom-free): [`targets/rocq/SizeDecomp.v`](targets/rocq/SizeDecomp.v).
