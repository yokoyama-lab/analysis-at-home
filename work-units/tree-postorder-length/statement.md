# Postorder traversal lists every node

## Claim
> `length (postorder t) = lsize t`.

## Cost model
Reference Rocq proof (`tree_postorder_length`, axiom-free): [`targets/rocq/PostorderLen.v`](targets/rocq/PostorderLen.v).
