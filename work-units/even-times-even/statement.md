# even * anything = even

## Claim
> `(exists k, a = 2*k) -> exists k, a*b = 2*k`.

## Cost model
Reference Rocq proof (`even_times_even`, axiom-free): [`targets/rocq/EvenTimes.v`](targets/rocq/EvenTimes.v).
