# Sum of squares of Fibonacci numbers = F_n * F_{n+1}

## Claim
> `ssq n = fib n * fib (S n)`.

## Cost model
Reference Rocq proof (`fib_sq_sum`, axiom-free): [`targets/rocq/FibSqSum.v`](targets/rocq/FibSqSum.v).
