# Filtering twice equals filtering once

## Claim
> `filter p (filter p l) = filter p l`.

## Cost model
Reference Rocq proof (`list_filter_idempotent`, axiom-free): [`targets/rocq/FilterIdem.v`](targets/rocq/FilterIdem.v).
