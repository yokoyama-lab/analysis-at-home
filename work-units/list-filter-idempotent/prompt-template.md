<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `filter p (filter p l) = filter p l` (mirror the Rocq target). Mirror
`work-units/list-filter-idempotent/targets/rocq/FilterIdem.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
