<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `rev (map f l) = map f (rev l)` (mirror the Rocq target). Mirror
`work-units/list-rev-map/targets/rocq/RevMap.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
