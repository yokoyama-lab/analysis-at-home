<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `hexs n + n = 2 * n * n` (mirror the Rocq target). Mirror
`work-units/hexagonal-numbers/targets/rocq/Hexagonal.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
