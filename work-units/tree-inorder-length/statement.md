# Inorder traversal lists every node

## Claim
> `length (inorder t) = lsize t`.

## Cost model
Reference Rocq proof (`tree_inorder_length`, axiom-free): [`targets/rocq/InorderLen.v`](targets/rocq/InorderLen.v).
