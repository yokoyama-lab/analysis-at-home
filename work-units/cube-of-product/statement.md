# (ab)^3 = a^3 b^3

## Claim
> `(a*b)*(a*b)*(a*b) = (a*a*a)*(b*b*b)`.

## Cost model
Reference Rocq proof (`cube_of_product`, axiom-free): [`targets/rocq/CubeOfProduct.v`](targets/rocq/CubeOfProduct.v).
