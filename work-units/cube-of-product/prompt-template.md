<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(a*b)*(a*b)*(a*b) = (a*a*a)*(b*b*b)` (mirror the Rocq target). Mirror
`work-units/cube-of-product/targets/rocq/CubeOfProduct.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
