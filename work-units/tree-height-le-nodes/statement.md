# Tree height is at most the number of internal nodes

## Claim
> `height t <= internal t`.

## Cost model
Reference Rocq proof (`tree_height_le_nodes`, axiom-free): [`targets/rocq/TreeHeight.v`](targets/rocq/TreeHeight.v).
