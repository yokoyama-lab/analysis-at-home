<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, that a binary tree's height is at most its internal-node count: prove `height t <= internal t`. Mirror
`work-units/tree-height-le-nodes/targets/rocq/TreeHeight.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
