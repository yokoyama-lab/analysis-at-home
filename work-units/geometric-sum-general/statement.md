# General geometric series sum

## Claim
> `gpow (S r) n * r + 1 = (S r) ^ n`.

## Cost model
Reference Rocq proof (`geometric_sum_general`, axiom-free): [`targets/rocq/GeomGen.v`](targets/rocq/GeomGen.v).
