# Law of exponents: (a*b)^n = a^n * b^n

## Claim
> `(a * b) ^ n = a ^ n * b ^ n`.

## Cost model
Reference Rocq proof (`power_mul_base`, axiom-free): [`targets/rocq/PowerOfProduct.v`](targets/rocq/PowerOfProduct.v).
