# Mirroring a tree visits each internal node once

## Claim
> `snd (mirrorc t) = internal t`.

## Cost model
Reference Rocq proof (`mirror_operations`, axiom-free): [`targets/rocq/TreeMirror.v`](targets/rocq/TreeMirror.v).
