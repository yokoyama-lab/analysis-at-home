<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (concat ls) = sumlen ls` (mirror the Rocq target). Mirror
`work-units/list-concat-length/targets/rocq/ConcatLen.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
