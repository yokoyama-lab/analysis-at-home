# concat length is the sum of lengths

## Claim
> `length (concat ls) = sumlen ls`.

## Cost model
Reference Rocq proof (`list_concat_length`, axiom-free): [`targets/rocq/ConcatLen.v`](targets/rocq/ConcatLen.v).
