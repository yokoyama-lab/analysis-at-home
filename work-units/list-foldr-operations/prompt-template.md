<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (foldrc f b l) = length l` (mirror the Rocq target's definitions). Mirror
`work-units/list-foldr-operations/targets/rocq/ListFoldr.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
