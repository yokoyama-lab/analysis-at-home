# Mirroring a tree twice is the identity

## Claim
> `mirror (mirror t) = t`.

## Cost model
Reference Rocq proof (`mirror_involutive`, axiom-free): [`targets/rocq/MirrorInvol.v`](targets/rocq/MirrorInvol.v).
