<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `length (l1 ++ l2) = length (l2 ++ l1)` (mirror the Rocq target). Mirror
`work-units/list-append-length-comm/targets/rocq/AppLenComm.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
