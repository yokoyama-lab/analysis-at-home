# Append length is commutative

## Claim
> `length (l1 ++ l2) = length (l2 ++ l1)`.

## Cost model
Reference Rocq proof (`list_app_length_comm`, axiom-free): [`targets/rocq/AppLenComm.v`](targets/rocq/AppLenComm.v).
