# A positive multiple is at least the divisor

## Claim
> `divides a b -> 0 < b -> a <= b`.

## Cost model
Reference Rocq proof (`divides_le`, axiom-free): [`targets/rocq/DividesLe.v`](targets/rocq/DividesLe.v).
