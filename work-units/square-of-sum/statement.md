# (a+b)^2 = a^2 + 2ab + b^2

## Claim
> `(a+b)*(a+b) = a*a + 2*a*b + b*b`.

## Cost model
Reference Rocq proof (`square_of_sum`, axiom-free): [`targets/rocq/SquareOfSum.v`](targets/rocq/SquareOfSum.v).
