<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, length l1 + length l2 <= fuel -> length (mergel fuel l1 l2) = length l1 + length l2 (mirror the Rocq target). Mirror
`work-units/merge-preserves-length/targets/rocq/MergeLength.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
