# Merging preserves total length

## Claim
> `length l1 + length l2 <= fuel -> length (mergel fuel l1 l2) = length l1 + length l2`.

## Cost model
Reference Rocq proof (`merge_preserves_length`, axiom-free): [`targets/rocq/MergeLength.v`](targets/rocq/MergeLength.v).
