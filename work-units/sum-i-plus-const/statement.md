# Sum of (i + c) for i=1..n

## Claim
> `2 * sic c n = n * (n + 1) + 2 * c * n`.

## Cost model
Reference Rocq proof (`sum_i_plus_const`, axiom-free): [`targets/rocq/SumIConst.v`](targets/rocq/SumIConst.v).
