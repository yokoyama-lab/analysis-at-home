<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `2 * sic c n = n * (n + 1) + 2 * c * n` (mirror the Rocq target). Mirror
`work-units/sum-i-plus-const/targets/rocq/SumIConst.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
