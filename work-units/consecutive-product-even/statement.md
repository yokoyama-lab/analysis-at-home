# Product of two consecutive integers is even

## Claim
> `exists k, n * (n + 1) = 2 * k`.

Why the triangular number `n(n+1)/2` is always an integer.

## Cost model
Reference Rocq proof (`consecutive_even`, axiom-free): [`targets/rocq/ConsecEven.v`](targets/rocq/ConsecEven.v).
