# Existential check uses at most n tests

## Claim
> `snd (anyc p l) <= length l`.

## Cost model
Reference Rocq proof (`list_any_tests`, axiom-free): [`targets/rocq/ListAny.v`](targets/rocq/ListAny.v).
