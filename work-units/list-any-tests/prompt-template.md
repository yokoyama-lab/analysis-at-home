<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the property `snd (anyc p l) <= length l` (mirror the Rocq target's definitions). Mirror
`work-units/list-any-tests/targets/rocq/ListAny.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
