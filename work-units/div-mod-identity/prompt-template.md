<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `b <> 0 -> a = b * (a / b) + a mod b` (mirror the Rocq target). Mirror
`work-units/div-mod-identity/targets/rocq/DivMod.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
