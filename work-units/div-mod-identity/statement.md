# Euclidean division: a = b*(a/b) + a mod b

## Claim
> `b <> 0 -> a = b * (a / b) + a mod b`.

## Cost model
Reference Rocq proof (`div_mod_identity`, axiom-free): [`targets/rocq/DivMod.v`](targets/rocq/DivMod.v).
