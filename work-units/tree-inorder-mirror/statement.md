# Inorder of the mirror is the reverse inorder

## Claim
> `inorder (lmirror t) = rev (inorder t)`.

## Cost model
Reference Rocq proof (`tree_inorder_mirror`, axiom-free): [`targets/rocq/InorderMirror.v`](targets/rocq/InorderMirror.v).
