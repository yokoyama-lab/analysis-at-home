<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `filter p (l1 ++ l2) = filter p l1 ++ filter p l2` (mirror the Rocq target). Mirror
`work-units/list-filter-append/targets/rocq/FilterApp.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
