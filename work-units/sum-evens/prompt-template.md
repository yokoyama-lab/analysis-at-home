<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, the identity `sumeven n = n * (n + 1)` (mirror the Rocq target). Mirror
`work-units/sum-evens/targets/rocq/SumEvens.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
