<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `mulc a b = length a * length b` (mirror the Rocq target). Mirror
`work-units/classical-multiplication/targets/rocq/ClassicalMul.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
