# Schoolbook multiplication uses n*m digit products

## Claim
> `mulc a b = length a * length b`.

## Cost model
Reference Rocq proof (`classical_multiplication_products`, axiom-free): [`targets/rocq/ClassicalMul.v`](targets/rocq/ClassicalMul.v).
