<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `(a+b)*(a+b)*(a+b) = a*a*a + 3*a*a*b + 3*a*b*b + b*b*b` (mirror the Rocq target). Mirror
`work-units/cube-of-sum/targets/rocq/CubeOfSum.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
