# Mirroring preserves height

## Claim
> `height (mirror t) = height t`.

## Cost model
Reference Rocq proof (`tree_mirror_height`, axiom-free): [`targets/rocq/MirrorHeight.v`](targets/rocq/MirrorHeight.v).
