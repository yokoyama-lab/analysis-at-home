# Radix sort: d linear passes = d*n operations

## Claim
> `loop d (loop n 1) = d * n`.

## Cost model
Reference Rocq proof (`radix_sort_passes`, axiom-free): [`targets/rocq/RadixPasses.v`](targets/rocq/RadixPasses.v).
