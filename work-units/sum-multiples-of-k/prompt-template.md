<!-- Porting task (lean | agda | isabelle). The Rocq target is verified. -->

---

Formalize and prove, in <BACKEND = lean | agda | isabelle>, `2 * smk k n = k * (n * (n+1))` (mirror the Rocq target). Mirror
`work-units/sum-multiples-of-k/targets/rocq/SumMultK.v`.

Return only the proof-assistant source, with no axioms / sorry / admit / postulate.
