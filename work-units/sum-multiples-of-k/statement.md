# Sum of the first n multiples of k

## Claim
> `2 * smk k n = k * (n * (n+1))`.

## Cost model
Reference Rocq proof (`sum_multiples_of_k`, axiom-free): [`targets/rocq/SumMultK.v`](targets/rocq/SumMultK.v).
