# Even + even = even

## Claim
> `(exists k, a = 2*k) -> (exists k, b = 2*k) -> exists k, a + b = 2*k`.

## Cost model
Reference Rocq proof (`even_plus_even`, axiom-free): [`targets/rocq/EvenPlusEven.v`](targets/rocq/EvenPlusEven.v).
