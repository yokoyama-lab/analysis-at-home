# Lucas numbers grow at most as 2^(n+1)

## Claim
> `lucas n <= 2 ^ (S n)`.

## Cost model
Reference Rocq proof (`lucas_le_pow`, axiom-free): [`targets/rocq/LucasLePow.v`](targets/rocq/LucasLePow.v).
