#!/usr/bin/env bash
# Lean adapter — STUB (Phase 3).
# Should compile the submission with `lake`/`lean`, then reject if it contains
# `sorry` or adds axioms (check `#print axioms`).
set -euo pipefail
printf '{"accepted": false, "reason": "lean adapter not implemented (stub)"}\n'
exit 3
