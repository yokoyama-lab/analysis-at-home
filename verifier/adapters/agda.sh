#!/usr/bin/env bash
# Agda adapter — STUB (Phase 3).
# Should type-check the submission with `agda`, then reject if it relies on
# postulates or `--allow-unsolved-metas`.
set -euo pipefail
printf '{"accepted": false, "reason": "agda adapter not implemented (stub)"}\n'
exit 3
